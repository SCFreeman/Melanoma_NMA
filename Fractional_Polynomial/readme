In this folder anova_parameterisation_FP.R is an R file which converts the IPD data into the aggregate format needed for the fractional polynomial models. 
The file calls in anova_data.R which contains the function for converting IPD to aggregate data. 
The resuting aggregate dataset is saved as melanoma_aggregate_data.csv.

Each sub-folder contains files for fitting the fractional polynomial model with different powers.
